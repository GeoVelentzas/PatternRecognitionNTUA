clear all; close all; clc;
load('DATA_NEW_FEAT');

CONDATA = [];

for i = 1:length(DATA)
    for j=1:length(DATA{i})
        CONDATA = [CONDATA ; DATA{i}{j}];
    end
end

[IDX] = kmeans(CONDATA);

for i = 1:length(DATA)
    for j=1:length(DATA{i})
        s = size([DATA{i}{j}],2);
        k = size([DATA{i}{j}],1);
        DATA{i}{j}(:,11)
    end
end 